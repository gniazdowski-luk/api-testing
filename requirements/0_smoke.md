# Smoke Requirements

## Users

### Users - Smoke

@smoke - Smoke tests:
- `checks that the endpoint for retrieving users is accessible and returns a successful response` - GET /users status is OK (status code 200).
